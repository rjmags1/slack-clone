import Button from '../../../lib/Button'

function WorkspaceSidebarMentionsReactionsBtn() {
    return (
        <Button
            className="flex w-full min-w-max justify-start truncate hover:underline"
            onClick={() => alert('not implemented')}
        >
            Mentions + reactions
        </Button>
    )
}

export default WorkspaceSidebarMentionsReactionsBtn
