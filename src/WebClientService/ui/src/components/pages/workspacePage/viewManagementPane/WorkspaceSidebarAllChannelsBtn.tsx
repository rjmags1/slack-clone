import Button from '../../../lib/Button'

function WorkspaceSidebarAllChannelsBtn() {
    return (
        <Button
            className="flex w-full min-w-max justify-start truncate hover:underline"
            onClick={() => alert('not implemented')}
        >
            All channels
        </Button>
    )
}

export default WorkspaceSidebarAllChannelsBtn
