function WorkspaceSidebarDirectMessagesListing({ item }: any) {
    return (
        <div
            className="px-2"
            onClick={() => alert('not implemented')}
        >{`${item.name}`}</div>
    )
}

export default WorkspaceSidebarDirectMessagesListing
